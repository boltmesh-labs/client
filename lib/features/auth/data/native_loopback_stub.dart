// Web stub: desktop loopback OAuth is unreachable without dart:io. The
// caller guards on the target platform, so this must never run.
Future<int> findFreeLoopbackPort() =>
    throw StateError('Loopback OAuth is not supported on this platform.');
