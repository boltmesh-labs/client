# boltmeshd owns boltmesh0 (wg-quick) and its DNS. Letting NetworkManager
# assume the external device makes GNOME report an activation failure on
# teardown ("Connection failed: Activation of network connection failed").
[keyfile]
unmanaged-devices=interface-name:boltmesh0
